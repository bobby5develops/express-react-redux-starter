import React, { PropTypes } from 'react';import Hero from './hero/Hero';import OffCanvas from './common/menu/OffCanvas';//TODO: refactor component to utilize parallax effect properly, and side-navigation menu should be offcanvasconst App = ({ children }) => {    return (        <div id="outer-container" className="container">            <Hero>{children}</Hero>			<OffCanvas />			{children}        </div>	);};App.propTypes = { children: PropTypes.object };export default App;